import {
} from '../actionTypes';

const initialState = {
};

const metadata = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default metadata;
